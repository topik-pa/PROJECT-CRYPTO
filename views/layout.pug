-
  let prod = process.env.NODE_ENV === 'production'
  let proj_name = 'project-crypto'
  let proj_tagline = 'Un progetto di test'
  let description = 'A description...'
  let proj_url = 'https://www.project-crypto.net'
  let copy = 2024
  let owner = 'Marco Pavan'
  let email = 'marco@progweb.it'
  let owner_email = 'marco@progweb.it'
  let logo = 'https://progweb.it/assets/images/logo.png'
  let sc_project = 12345
  let sc_security = 'xxxxx'  
  let lang = locale

doctype html
html(lang=lang)
  head
    block meta 
      meta(charset="utf-8")
      meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
      meta(name="author" content=owner)
      link(rel="shortcut icon", href="/public/favicon.ico", type="image/x-icon")
    script(nonce=nonce).
      if (localStorage.getItem('dark')) document.documentElement.classList.add('dark')
    if prod
        link(rel="stylesheet", type="text/css", href="/dist/main.css")
    else
        link(rel="stylesheet", type="text/css", href="/styles/style.css")
  body(id=id, class=className)
    include ../components/shared/header/header.pug
    block intro
    main 
      block main
    block outro
    block footer
      include ../components/shared/footer/footer.pug
      include ../components/shared/cookie_layer/cookie_layer.pug
      include ../components/shared/goto_top/goto_top.pug
    if prod
      script(src='/dist/main.js')
    else
      script(src='/scripts/index.js', type='module')
    if prod
      script(nonce=nonce).
        var sc_project = #{sc_project};
         var sc_invisible = 1;
         var sc_security = "#{sc_security}";
      script(async, defer, src='https://www.statcounter.com/counter/counter.js')
      script(type='application/ld+json').
          {
            "@context": "https://schema.org",
            "@type": "WebSite",
            "url": "#{proj_url}",
            "name": "#{proj_name}",
            "description": "#{description}",
            "image": "#{logo}",
            "author": {
              "@type": "Person",
              "name": "#{owner}"
            },
            "copyrightHolder": {
              "@type": "Person",
              "name": "#{owner}"
            }
          }

